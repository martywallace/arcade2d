{
  "name": "@arcade2d/demo-dungeon-crawler",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "yarn workspace @arcade2d/root exec turbo run build --filter=@arcade2d/demo-dungeon-crawler^... && vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@arcade2d/engine": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "vite": "^7.1.2"
  }
}
