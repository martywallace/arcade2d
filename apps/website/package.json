{
  "name": "@arcade2d/website",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "yarn workspace @arcade2d/root exec turbo run build --filter=@arcade2d/website^... && node scripts/sync-demos.mjs && astro dev",
    "build": "node scripts/sync-demos.mjs && astro build",
    "preview": "astro preview",
    "typecheck": "astro check",
    "lint": "astro check",
    "deploy": "yarn build && wrangler deploy"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "astro-icon": "^1.1.5"
  },
  "devDependencies": {
    "@arcade2d/demo-td-shooter": "workspace:*",
    "@astrojs/check": "^0.9.0",
    "@iconify-json/lucide": "^1.2.0",
    "typescript": "^5.9.2",
    "wrangler": "^3.90.0"
  }
}
